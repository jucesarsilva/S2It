'use strict';

describe('S2it.master module', function() {

  beforeEach(module('S2it.master'));

  describe('master controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var MasterController = $controller('MasterController');
      expect(MasterController).toBeDefined();
    }));

  });
});